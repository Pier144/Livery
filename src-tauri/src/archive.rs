//! Archives (M4): list zip/rar/7z entries, locate `<vehicle>.blk` files, detect the vehicle and
//! conflicts, extract to a temp dir and move into `UserSkins` atomically. Emits `install://progress`.
