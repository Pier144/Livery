# Design notes (living document)

Record here every decision taken during implementation that the handoff left open, plus any token or copy added.

| date | topic | decision | why |
|---|---|---|---|
| 2026-09-19 | Repo layout | Handoff docs moved to `design_handoff_livery/`; `CLAUDE.md`, `DESIGN_NOTES.md` and `tokens/` stay at the root (the app reads/updates them). App scaffolded at the root. | Kickoff assumed this layout; keeps design refs apart from code. |
| 2026-09-19 | Tooling versions | React 18.3, TypeScript 5.9 (not 7), Vite 8, Vitest 5, Tailwind 3.4, Tauri 2.11, Zustand 5, TanStack Query 5 / Virtual 3, react-i18next 17, lucide-react 1.x. | Stack fixed by CLAUDE.md; TS 5.9 chosen over the new native TS 7 for ecosystem compatibility. |
| 2026-09-19 | Dependencies approved beyond the stack | jsdom, @testing-library/jest-dom, @testing-library/user-event, axe-core (dev). Fonts copied from `geist@1.7.2` and `@fontsource/ibm-plex-mono@5.3.0` (not kept as deps). Rust: serde/serde_json (Tauri template), tracing + tracing-subscriber (BUILD_PLAN M0). | Approved by the author on 2026-09-19. |
| 2026-09-19 | Tauri plugins deferred | dialog, opener (shell-open), os, fs and updater are **not** added in M0. M1 uses only core window/webview APIs. Each plugin will be proposed in the milestone that needs it (dialog/opener → M2, updater → M6, which also needs a signing key). | Author did not approve the plugin group yet. |
| 2026-09-19 | M2 · Dependencies approved | `tauri-plugin-dialog` 2 (+ `@tauri-apps/plugin-dialog`, capability `dialog:allow-open`) for the folder picker; `winreg` 0.56 (Windows only) for the Steam registry keys. Still pending approval: `rusqlite`, archive crates (`zip`, `unrar`, `sevenz-rust`), `notify`, an HTTP client and `scraper` for WT Live, `tauri-plugin-opener`, `tauri-plugin-updater`, `tauri-plugin-autostart`. | Author approved on 2026-09-19. |
| 2026-09-19 | Fonts | Geist 400/500/600 (static woff2); IBM Plex Mono 400/500 in latin, latin-ext and cyrillic subsets with `unicode-range`. `↵` (U+21B5) is not in Plex Mono's latin subset and falls back to the system mono. | Cyrillic needed for the Русский language option. |
| 2026-09-19 | Tokens added | `bg.scrim` rgba(19,20,22,.85) (drop overlay), `bg.skel` #222327 (skeleton title bar, from prototype), `backgroundSize.shimmer` 200% 100%, `backgroundPosition.grid` -1px -1px, `fontSize.heading-lg` 18px/1.3 500 (empty-state and dialog titles). | Values used by the prototype but missing from the token file. |
| 2026-09-19 | Ruled grid utility | `bg-grid` emits image + 28px size + -1px position in one class (three token groups share the key). | One class per content area. |
| 2026-09-19 | Settings shortcut | Keys 1–5 switch sections (5 = Settings) and `,` also opens Settings, matching the sidebar hint. `[` and `]` both toggle the sidebar (prototype behaviour). | README lists "1–5" but the sidebar shows `,` for Settings. |
| 2026-09-19 | Sidebar preference | `sidebarOpen` is persisted in localStorage (`livery.ui`); nothing else in the UI store persists. | Per-user convenience; not worth a backend setting. |
| 2026-09-19 | Settings file | `<appData>/settings.json`, written atomically (temp + rename). Unknown fields ignored, missing fields defaulted; a corrupt file is renamed to `settings.json.bad` and defaults are used. | Never crash on a bad settings file. |
| 2026-09-19 | App identifier | `app.livery.desktop`. | Neutral reverse-DNS id; change before the first public release if a domain is registered. |
| 2026-09-19 | Vehicle list | `src/data/vehicles.json` holds a 9-vehicle placeholder seed (the prototype's sample vehicles) for the palette and the Explore filters. The datamined list waits for the author to choose a source and check its licence. | BUILD_PLAN open decision 3 is still open (see HANDOFF.md). |
| 2026-09-19 | App icon | Amber dot (#F7A224 ≈ oklch(0.78 0.16 70)) with glow on a graphite tile with a faint ruled grid; source `src-tauri/icons/app-icon.svg`. | Design system names the concept but has no asset. |
| 2026-09-19 | M1 · Dividers | Inner dividers use `line-2`. The prototype uses an untokenised #222327 for these (e.g. the title-bar bottom border), but the README specifies line-2. | README wins. |
| 2026-09-19 | M1 · Title bar height | `box-content h-titlebar`: 38px of content plus a 1px border, 39px in total, as in the prototype. The 26px controls then sit on whole pixels (y=6). | Crisp rendering; matches the prototype. |
| 2026-09-19 | M1 · Window controls | Lucide `minus` / `square` / `x`. When the window is maximized the middle button keeps `square` but its label/tooltip becomes "Restore". Native-style `title` tooltips. Failures of the native call are swallowed. The maximized state is re-read on resize and the newest answer wins. | README icon list; out-of-order `isMaximized()` answers used to show the wrong label. |
| 2026-09-19 | M1 · Drag region | `data-tauri-drag-region` sits on the header and every non-interactive wrapper/text in it, never on buttons. Double-click maximizes natively. | Tauri 2 applies the attribute only to the element that carries it. |
| 2026-09-19 | M1 · Sidebar width | 216px / 56px **border-box** (README, `w-sidebar`). The prototype's inline `width:216px` is content-box, so it renders 237px / 57px. Nav items are therefore 195px wide instead of 216px. | README wins. |
| 2026-09-19 | M1 · Sidebar icons | Lucide icons in the expanded items and a 36×34 icon rail when collapsed. The prototype shows text only and mono initials. The collapsed rail shows no amber left bar (as in the prototype); `aria-current` marks the active item. | README wins. |
| 2026-09-19 | M1 · Sidebar a11y | Key hints are aria-hidden and exposed through `aria-keyshortcuts`. The queue badge count is read through `aria-describedby` ("2 archives waiting"). The collapsed status dot is `role=img` with the status as its name. The toggle keeps focus across collapse/expand. | Clean accessible names. |
| 2026-09-19 | M1 · Contrast | Uses `ink-4` instead of the prototype's `ink-5` for the sidebar "Hangar …" line, the "Collapse sidebar [" button and the palette placeholder. `ink-5` (≈3.6:1 on bg-1) stays only on supplementary hints: sidebar key hints, palette kind column and footer. | WCAG AA 4.5:1 for real text and controls; README contrast table reserves ink-5 for hints. |
| 2026-09-19 | M1 · WT Live status | **Updated in M5:** the count is the first page's `total` of the newest cached **unfiltered** Explore search. Sidebar reads the query cache through `useSyncExternalStore`, never starts a request, and keeps the last value per QueryClient. Before Explore has run, or when only filtered searches have run, it shows "WT Live online"; offline, "WT Live offline". The full "WT Live online · 2,318 skins" wraps to two lines in the 174px card, and does so in the prototype too. | No data before M5. |
| 2026-09-19 | M1 · Italian nav label | `common.nav.queue` in Italian is "Da installare". The screen title and the palette action keep "Coda di installazione". | "Coda di installazione" (126px) truncates next to a two-digit badge (99px available). The author may prefer other wording. |
| 2026-09-19 | M1 · Palette results | Action hints show the section shortcut (1–5, read from `SECTION_KEYS`) instead of the prototype's meaningless "Action". Empty query: 5 actions + 4 vehicles. Otherwise skins → vehicles → actions, case-insensitive substring on label or hint, max 9. Vehicles go to Explore (the vehicle filter arrives in M5), and skins arrive in M5. | README leaves the action hint open. |
| 2026-09-19 | M1 · Palette a11y | ARIA combobox + listbox (`aria-activedescendant`); rows are `role=option` divs, not tab stops. Tab is trapped in the input and focus returns to the opener on close. The input has no outline: the caret plus the highlighted row are the focus indicator in this modal. "No matches" sits in a `role=status` region outside the listbox. The overlay closes on mousedown on itself. | WAI-ARIA pattern; a listbox may only contain options. |
| 2026-09-19 | M1 · Escape layering | Escape inside the palette closes only the palette and stops propagation. Later menus, dialogs and zoom must also close only the topmost layer. | Avoid closing what is underneath. |
| 2026-09-19 | M1 · Toasts | Always-mounted `<section aria-live="polite">`; newest at the bottom; max 4, oldest dropped. Each toast's 6s timer pauses while hovered or focused and resumes with the time left. Keyboard Undo/× hands focus to the next toast or back to where it was. `undo()` runs at most once, never after expiry. | WCAG 2.2.1 (timing adjustable) and focus management. |
| 2026-09-19 | M1 · Ctrl+Z | **New shortcut**: Ctrl/Cmd+Z (outside text inputs) undoes the newest toast that offers Undo. | Keyboard users otherwise have to Tab to the end of the page within 6s. |
| 2026-09-19 | M1 · Drag & drop | Inside Tauri uses `onDragDropEvent` (real OS paths); in the browser falls back to HTML5 events (file names only). A drop with at least one ZIP/RAR/7z → items `analyzing` + switch to Install queue, no toast. A drop with no archive → "Only ZIP, RAR and 7z archives can be added" toast (the prototype's fake `dropped_archive.zip` fallback is not copied). A Tauri `enter` with no file paths does not show the overlay. The overlay also clears on window blur. Later changes: during First run drops don't switch screens (M2 · First run drops), and since M4 every dropped path is analysed (M4 · Queue UI). | Real paths are needed by `analyze_archive` (M4). |
| 2026-09-19 | M1 · Kbd / line-height | Kbd, the palette rows and the offline tag use `leading-[normal]` to match the prototype's `font:` shorthand (17px key cap, 35px palette rows). | 1:1 with the prototype. |
| 2026-09-19 | M1 · Motion | Hover color transitions (120ms) use `motion-safe:` variants, as does the toast enter animation (`toastIn`). | README Motion; instant under reduced motion. |
| 2026-09-19 | M2 · Real-world facts | Verified on the author's machine and the WT wiki. Steam's registry `SteamPath` is lower-case with forward slashes. War Thunder (appid 236390) lives in a secondary library listed in `libraryfolders.vdf`. The root holds `launcher.exe` + `win64\aces.exe` (no `aces.exe` at the root) and `content\pkg_main.ver` (e.g. `2.59.0.13`); there is no `version` file. `%LOCALAPPDATA%\WarThunder` can hold only `downloads\` and is not a root. The in-game template folder is `template_<vehicle>` holding `<vehicle>.blk`; the game shows the folder name as the skin name. | DATA_MODEL assumed `aces.exe` at the root and a `version` file. |
| 2026-09-19 | M2 · Game root | Valid root = `UserSkins\` or `launcher.exe` or `aces.exe` or `win64\aces.exe`. Picking `UserSkins`, `win64`, `win32`, `content`, `content.hq` or a file inside the root resolves to the root. Version = `content\pkg_main.ver` (fallback: a root `version` file), shown as major.minor. `set_game_path` creates `UserSkins\` only when the user confirms, and a new root without a version clears the stored one (`SettingsPatch.gameVersion` is a double option on the Rust side). | Real installs. |
| 2026-09-19 | M2 · Detection | Steam roots come from the registry (HKCU `SteamPath`, HKLM `InstallPath`) plus the default path. Libraries come from `steamapps/libraryfolders.vdf` (new and legacy formats), falling back to `config/`. `appmanifest_236390.acf` gives `installdir`. Standalone candidates: `%LOCALAPPDATA%\WarThunder`, Program Files (x86) and Program Files `\WarThunder`, `…\Gaijin\War Thunder`, `C:\Games\WarThunder`. Every source is checked and emits `game://detect` (`checking` → `found`/`notFound`; custom without a saved path → `skipped`, a new state in `types.ts`/`model.rs`; DATA_MODEL.md belongs to the handoff spec and isn't edited). Result priority: Steam > standalone > custom. Full paths are logged only at debug level. | Each First run row shows a real outcome. |
| 2026-09-19 | M2 · Detection pacing | Real results arrive in milliseconds. The UI reveals them over ~1.2s (Steam at 30%, standalone at 70%, custom at 100%, as in the prototype) and then auto-advances. Events and the command reply may arrive in any order; missing final states are filled in from the reply. | Readable, honest progress. |
| 2026-09-19 | M2 · Onboarding flag | New setting `onboarded` (not in DATA_MODEL). First run shows when `!onboarded && !gamePath`. Finishing, skipping the import or "Skip for now" sets it. While settings load, the shell shows the title bar and an empty ruled main (no sidebar flash). If settings can't be read, the app boots to Explore. | Skipping must not bring First run back every launch. |
| 2026-09-19 | M2 · First run shell | No sidebar. Keys 1–5, `,` and `[ ]` are ignored; Ctrl+K, Esc and Ctrl+Z still work (prototype). Leaving through the palette doesn't set `onboarded`. | Prototype lines 632-638. |
| 2026-09-19 | M2 · First run UI | Past steps ink-3 and future steps ink-5 (README). Confirm card border `amber-60` (README; the prototype uses .5). "Show path" reveals the game root. A folder that is not a game shows an inline `role=alert` message. The "Game folder set" toast appears only for a hand-picked or dropped folder. With 0 existing skins First run finishes right after confirming. Focus moves to each step's heading. | README wins; a11y. |
| 2026-09-19 | M2 · First run drops | While the not-found view is shown, window drops go to it (`ui.folderDrop`) and the overlay reads "Drop the War Thunder folder · FOLDER". During First run otherwise, archives are queued without switching screens, with a new "N archives added to the install queue" toast. | The prototype gave no feedback (WCAG 4.1.3). |
| 2026-09-19 | M2 · Technical animation | 1:1 port of the prototype's drawing. Per-element delays are inline `animationDelay` (data-driven, and it survives the `motion-safe` shorthand); 3D geometry is inline. "UNOFFICIAL TOOL" is end-anchored at x=482 so longer translations stay on the sheet. Below 1280px the stage is scaled to 90%. Reduced motion shows the final frame. | Fits 1100×700; i18n. |
| 2026-09-19 | M2 · Skin scan | One sub-folder of `UserSkins` = one skin. Folders starting with `.` are skipped (Livery's own `.livery\` lives there from M3). The blk is the alphabetically first top-level `*.blk`; its stem is the vehicle code. Attention: `.livery-partial` marker → partialExtract; no blk → noBlk; unreadable blk → **unreadableBlk** (new kind, message carries line:column); root blocks other than `replace_tex`/`set_tex` → unknownBlkBlock; a `to:` texture missing from the folder → missingTexture (a trailing `*` is stripped; without an extension, `.dds` or `.tga`). Origin is `mine` for `template_` folders, otherwise `imported`. The name is the folder name. | Real skin layout. Known limit: a `to:` pointing at a built-in game texture is a false missingTexture. |
| 2026-09-19 | M2 · Vehicle fallback | Codes missing from `src/data/vehicles.json` (embedded in Rust) get name = code, nation from the prefix (`germ_` GER, `us_` USA, `ussr_` USSR, `uk_` GBR, `jp_` JPN, `cn_` CHN, `it_` ITA, `fr_` FRA, `sw_` SWE, `il_` ISR, otherwise **`UNK`**, a new Nation value), type ground when prefixed (naval for ship words), otherwise air. | Heuristic until the M5 datamined catalog. |
| 2026-09-19 | M2 · Library index | `<appData>/library.json` (`{ version: 1, skins }`), atomic writes, tolerant load (corrupt → `.bad`). `scan_user_skins` never writes; `import_skins` upserts by folder name (case-insensitive) and returns the whole index. Ids `s-<hex nanos>-<n>`; not-yet-imported folders are `disk:<folder>`. JSON rather than SQLite because `rusqlite` is not an approved dependency yet (README allows "SQLite (or JSON)"). | Dependency rule in CLAUDE.md. |
| 2026-09-19 | M2 · BLK parser | `library::blk` (reused by M4 archives): `name:type=value` params, blocks, `include`, quotes `"…"`, `'…'`, `"""…"""`, DAGOR `~` escapes, `;`, newline or space separators, `//` and `/* */` comments, BOM, errors with line:col, nesting limit, linear time on hostile input. | Fixture-tested. |
| 2026-09-19 | M2 · Blocking work | Disk-touching commands are `async` and run in `spawn_blocking` through the shared `crate::blocking` helper. | The UI thread never waits on I/O. |
| 2026-09-19 | Tokens | `ink-4` is **#80828a** (handoff #7c7e86): 4.59:1 on bg-3 and 4.63:1 on inputs (was 4.34 and 4.38), and ≥4.76 on bg-1/2/status. The change is invisible to the eye. New tokens: `bg.tile`, `placeholder-thumb`, `placeholder-tile`, `hatch`, `scanline` backgrounds, and the `draw`, `label-in`, `tile-in` animations. | WCAG AA for small text everywhere ink-4 sits. |
| 2026-09-19 | Buttons | At 34px, secondary and ghost buttons use 14px side padding and primary 16px (prototype). | 1:1 widths. |
| 2026-09-19 | M3 · Inactive skins (open decision 1) | An inactive skin's folder moves to `UserSkins\.livery\inactive\<folder>` (atomic rename on the same volume; the game only looks for `<vehicle>.blk` directly inside each `UserSkins\<folder>`, so it ignores `.livery`). The folder name, which the game shows as the skin name, is preserved. `active` is whatever the disk says. | Instant toggles and no renamed skins in game; a `_` prefix would still show in game under a changed name. |
| 2026-09-19 | M3 · Backups | Before a delete, the folder moves to `UserSkins\.livery\backups\<backupId>\<folder>` (same volume, instant Undo). With "Keep a backup" off the backup is **ephemeral**: Undo-only, not listed, purged after 60s. Kept backups expire after `backupDays` (30). Purges run at launch and at the start of every library command. `restore_backups` puts skins back where they were with their original id; on a name clash it restores as `<folder> (n)`. | CLAUDE.md: every regrettable change is undoable with a backend backup. |
| 2026-09-19 | M3 · Library index v2 | (Since M6 there is one file per game folder: see "M6 · Library per game folder".) `library.json` = `{ version: 2, skins, collections, activeCollectionId, backups }` (v1 loads transparently). All changes go through `LibraryStore::transact`, which journals renames and undoes them if anything fails, so disk and index never disagree. Every folder name from the index is validated before building a path (rejects `..`, trailing dots/spaces, hidden names), so a tampered index can't touch anything outside `UserSkins`. Re-check refreshes and prunes the index (not when `UserSkins` is missing). | Data safety. |
| 2026-09-19 | M3 · Partial failures | Toggle/activate move what they can; clashing folders are skipped and reported in one error (conflict > io > notFound, names one per line in `detail`). Delete is all-or-nothing. The UI re-reads the index and collections after any such error. | Clear outcome, consistent UI. |
| 2026-09-19 | M3 · Export | Copies each skin folder into a folder the user picks (`<folder> (n)` on clashes). ZIP export waits for approval of a zip crate. | Dependency rule. |
| 2026-09-19 | M3 · Collections | Stored in the index. Activating a collection makes exactly its skins active (folders moved as above); nothing is deleted. Delete collection and Remove member are undoable (restore / re-add). Activation is a plain toast (spec). Open question: `activeCollectionId` stays set after manual toggles, and an Undo of a deleted active collection doesn't re-activate it. | README §5. |
| 2026-09-19 | M3 · Hangar interaction | **Updated in M5:** a skin from WT Live (`sourceId`) opens its Skin detail on click or Enter, as in the prototype. Space, Ctrl/Cmd+click, the checkbox and Shift select it. A local skin (no `sourceId`) toggles selection on click, Enter and Space, because the prototype's texture viewer isn't built. Each card's `aria-describedby` points at a hidden hint explaining which applies. Shift extends a range. The checkbox is not a tab stop (the card is). Bulk actions apply only to selected skins that the current filters show. Active toggles are optimistic. The bulk bar is a `role=toolbar` placed before the list in DOM order; Escape clears the selection. The card's `role=button` is a full-card hit area under the inner controls (avoids nested-interactive). | No detail screen yet; keyboard efficiency; a11y. |
| 2026-09-19 | M3 · Hangar virtualization | TanStack Virtual over a flattened row model (group headers + card lines chunked by container width), with a pinned group header overlay. Measured with 1,000 skins (`pnpm dev:mock` + `?many=1`): ~20 cards in the DOM, p95 frame 14ms while scrolling. | BUILD_PLAN M3 acceptance (60fps). |
| 2026-09-19 | M3 · Hangar filters | Chips list only values present in the hangar, plus the currently set one. The "any" labels are per chip so Italian agrees in gender: "Tutte le nazioni", "Tutti i tipi", "Tutte le origini". Nation display names are under `hangar.nation.*`. | Natural Italian. |
| 2026-09-19 | M3 · Contrast tweaks | Hangar: "Inactive" and sizes on selected rows use ink-3 (ink-4 on bg-4/bg-hover is 4.2–4.4:1); group codes use ink-4. Unchecked checkbox border ink-5 (was line-4, 1.5:1) and the off switch track gets an ink-5 inner ring: both reach 3:1 non-text contrast (WCAG 1.4.11). | AA. |
| 2026-09-19 | M3 · UI primitives | `Menu` (APG menu button: arrows, Home/End, type-ahead, Escape closes only the menu, Tab closes), `ChipDropdown` (`labelValue` for Explore, `label` for Hangar; amber-60 border when set in both), `SegmentedControl` (radiogroup, roving tabindex), `Checkbox` (`role=checkbox`, 24px hit halo), `Switch`, `TextInput` (Escape clears a non-empty search). Menu min-width is 160px: the prototype's 150px is content-box. | Reused by Explore and Settings. |
| 2026-09-19 | Dev · Mock backend | `pnpm dev:mock` (`vite --mode mock`) answers every command in the browser from `src/dev/mockBackend.ts` with the prototype's sample data and 80–250ms latency. Switches (query param or `livery.mock.*` localStorage): `?onboarded=1`, `?empty=1`, `?notfound=1`, `?many=1` (1,000 skins). Only reachable through a dynamic import behind `VITE_MOCK_BACKEND`, so production builds don't contain it (checked). | Visual comparison with the prototype without the desktop app. |
| 2026-09-19 | M4 · Folders now, archives later | ZIP/RAR/7z crates and `notify` aren't approved yet. The queue installs **skin folders** today (drop a folder or "Choose a skin folder"), behind a `SkinSource` trait (`FolderSource` now; Zip/Rar/SevenZ sources plug into `open_source` later). A dropped archive becomes an error row with code `unsupported` and a short explanation; the drop zone shows a one-line note until `ARCHIVES_SUPPORTED` flips. | Dependency rule in CLAUDE.md. |
| 2026-09-19 | M4 · Analysis | Skin roots are directories (source root or sub-directories, depth ≤ 4) with a top-level `*.blk`. One root → ready; several → needsLook (pick a vehicle); none → error. Target folder = the root's name, sanitized; Windows device names (`CON`, `COM1`, `LPT¹`… before the first dot) get `_` right after the reserved part. Conflicts: the name is taken in `UserSkins`, in `.livery\inactive`, or by another queued item (`conflictWith = queue:<id>`, shown as "(queued)"). The same path isn't analysed twice. | Real skin packs. |
| 2026-09-19 | M4 · Install | Copy into `UserSkins\.livery\partial\<installId>\<folder>` with a `.livery-partial` marker, verify (file count and bytes, then attention scan), then one atomic rename into place (retried briefly for antivirus locks). Staging is cleaned on every failure and at launch. `install://progress` is throttled to ≤ 30/s with forced 0/80/95/100 boundaries; the final event is always sent. The UI batches progress with requestAnimationFrame plus a 100ms fallback for hidden windows. | Atomic installs; responsive UI. |
| 2026-09-19 | M4 · Conflicts | The policy is the dialog choice, else Settings → Conflicts; `ask` without a choice → `conflict` error and the dialog opens. Replace backs up the old skin (reason replace) and puts the new one in the same place and state. Undo (toast) = `undo_replace`: the new copy is removed (its source still exists) and the old one comes back under its original folder and id. Copy → `<folder> (n)`. Skip → nothing changes and the row leaves the queue (done + message `skipped`). | README §6; Undo for regrettable changes. |
| 2026-09-19 | M4 · Watcher | Polling every 2s with std (no `notify`). Watched folder (default Downloads) only while "Watch Downloads folder" is on (`autoInstall`): new `.zip/.rar/.7z` or skin folders whose size is stable across two polls ≥ 1s apart; browser temp files (`.part`, `.crdownload`…) and anything present when watching starts are ignored. Ready items auto-install with Settings → Conflicts (never auto-replace under Ask). `UserSkins` changes (names + mtimes, ignoring `.livery\partial`) emit `hangar://changed`, and the UI rescans so the index follows the disk. `watch_folder` validates the folder (not inside UserSkins) and returns Settings. | Works without the notify crate; notify can feed the same pollers later. |
| 2026-09-19 | M4 · Textures | DDS (FourCC and DX10/DXGI) and TGA headers from the first 148 bytes. Labels BC1–BC7, BC6H, RGBA8/RGB8 (BGRA shares the RGBA label), R8… One warning per texture, most serious first: unreadable header → not a square power of two in 128–8192 → heavier than 4096. Missing blk references are listed with "Referenced in skin.blk but not in the archive." The blk itself is listed last as BLK. `read_textures` works for hangar skins and queue items. | README Textures tab (M5). |
| 2026-09-19 | M4 · Queue UI | Any dropped path is analysed. "Not a skin" paths vanish with one toast for the whole drop; other failures stay as error rows. "Install N ready" runs sequentially with one summary toast. Needs a look → "Pick vehicle" menu. The dialog is 520px border-box (prototype 570 content-box), with a focus trap; Escape = Cancel. New token `amber.18` for the Replace option and Resolve hover. The Watch switch drives `watch_folder` (= autoInstall). | README §6. |
| 2026-09-19 | M4 · Known gaps | A pack holding two skins for the same vehicle can only install the first by path (`install_from_archive` picks by vehicle code; needs a `targetFolder` argument). `undo_replace` deletes the new copy without a backup because its source still exists. Watch-folder error messages come from the backend in English. | To revisit. |
| 2026-09-19 | M5 · Foundation contracts | Not in DATA_MODEL: `Settings.reduceMotion` (`system` / `on` / `off`, default `system`); `following_list` / `following_set` / `following_mark_seen` over `<appData>/following.json` (Following is local data); `wtlive_search` takes `{ params }`; `finalize_try`'s `skinId` is the **WT Live** id (the hangar skin's `sourceId`). WT Live counts as offline when a call fails with `network`, `unsupported` or `noBackend` (`isOfflineError`), or when the backend emits `net://status { online: false }` (`useNetStatusEvents`, mounted in App). | Contracts the M5/M6 parts share. |
| 2026-09-19 | M5 · WT Live installs | Tracked in `src/store/installs.ts`, keyed by WT Live id. A finished install stays at 100% until the hangar refetch shows it (no flash back to Install), then toasts "Installed “name”". A Try in game install doesn't toast, because the Try tab has its own UI. | README Interactions: "on Done, skin joins My Hangar and toasts". |
| 2026-09-19 | M5 · Temporary installs | Try in game skins (`HangarSkin.temporary`) are left out of `useHangar()` by default, so they don't show in My Hangar, Collections or the sidebar count. `useHangar({ includeTemporary: true })` is for the WT Live install state, the Skin detail and the queue's folder-conflict hints. | "Nothing is added to My Hangar until you press Keep." |
| 2026-09-19 | M5 · Explore paging (open decision 2) | We use the recommended option. When HTTP is approved, the backend fetches the first 5 listing pages, prefetches the rest in the background, caches them and filters locally. The frontend asks for pages of results (`useWtLiveSearchPages`, an infinite query) and loads the next one when the virtual grid nears the end. The count and ms come from the first page. | Local-filter speed without waiting for the whole catalogue. |
| 2026-09-19 | M5 · WT Live client seam | `wtlive/client.rs`: a `WtLiveClient` trait (`ready`, `search`, `post`, `following_new`, `download`). Its methods are blocking and are called through `crate::blocking`. It is held as `Arc<dyn WtLiveClient>` in the managed `wtlive::WtLive { client, following }`. `WtLive::load` uses `DisabledClient` today; the real client plugs in there and the commands stay the same. `ready()` makes no request, so an install fails at once instead of handing out an installId that can only fail. | The HTTP client can land without touching the commands. |
| 2026-09-19 | M5 · Offline answers | Until HTTP is approved, `wtlive_search/post/following_new`, `install_from_wtlive`, `finalize_try` and `read_textures { wtliveId }` answer `unsupported` with `wtlive::UNSUPPORTED_MESSAGE`. Every WT Live outcome emits `net://status` (success → online, network/unsupported → offline), not only on change, so a reloaded webview resyncs. Other errors (invalidInput, notFound) emit nothing. Once a client is ready, install and textures-before-install still need the archive crates (`UNSUPPORTED_ARCHIVES`). | The designed offline state instead of an internal error. |
| 2026-09-19 | M5 · WT Live plan (module doc) | Recorded in `wtlive/mod.rs`: Livery/<version> user agent, 1 req/s, a `Parser` trait tested against saved-HTML fixtures in `src-tauri/tests/fixtures/wtlive/` (captured once HTTP is approved), a 24 h JSON cache under `<appData>/wtlive-cache/` (served stale when offline), open decision 2 as below, and the temporary-install lifecycle (see "M5 · Temporary installs"; leftovers purged at launch by a future `wtlive::purge_on_startup`). | One place for the next step. |
| 2026-09-19 | M5 · Following store | `<appData>/following.json` = `{ version: 1, entries }`. Entries are keyed by (kind, id) and kept in follow order. Following again keeps `lastSeenAt` and refreshes the name. Unfollowing something not followed is a no-op. id and name are trimmed; a blank one is invalidInput. `mark_seen` sets every `lastSeenAt` to now. Writes happen only on change and are atomic; memory changes only after a successful write. Loading is tolerant: corrupt → `.bad`, bad entries dropped (copy kept as `.bad`), bad dates reset, BOM/unknown fields/newer version accepted. `following_new` passes each followed entry with its own `lastSeenAt` to the client. | Follows are local data and work offline. |
| 2026-09-19 | M5 · read_textures | Takes exactly one of `skinId`, `queueId`, `wtliveId`; anything else is invalidInput "Pass exactly one of a skin id, a queue id or a WT Live id". | One command for Hangar, Queue and Skin detail. |
| 2026-09-19 | M2/M5 · Settings commands | `get_settings` and `set_settings` are async. `get_settings` only reads memory; `set_settings` writes in `crate::blocking`. `useUpdateSettings` runs its mutations in one scope so patches keep their order. | DESIGN_NOTES "M2 · Blocking work". |
| 2026-09-19 | Dev · Mock backend (M5) | 16 WT Live posts s1–s16 from the prototype's CATALOG (every vehicle in `vehicles.json`; `images: []`, so the UI shows its placeholder views). Search ANDs every filter; pages hold 60; `tookMs` = 12 + items on the page. `?many=1` also makes 1,284 posts. `?empty=1` also empties Following. `?offline=1` / `livery.mock.offline` (read on every WT Live call, so it can be flipped at runtime) rejects WT Live calls with `network` and emits `net://status`. The install progress streams over ~3 s. Folders are named `<code>_<author>`; a different post landing on the same folder installs into `<folder> (2)`. Conflicts (same post already installed) follow the queue's rules. `finalize_try` is local and emits no status. | Explore/Detail can be exercised in the browser. |
| 2026-09-19 | M5 · Explore layout | The card is a `role=group` with a full-card `role=button` hit area under Install/Retry/Install as a copy (like the Hangar card), so nothing interactive is nested. The header count '{total} results · {ms} ms' comes from the first page, shows only on the Explore tab with results, and is a `role=status`. The Following pill counts distinct new skins (3 with the mock; the prototype's 4 counts one skin twice) and is hidden at 0. The Explore store is UI-only and not persisted; it survives a trip to the detail. `applyVehicle` / `applyAuthor` switch to the Explore tab, clear the other filters and keep the sort. | Accessible structure; a focused view when coming from Following, the palette or the detail. |
| 2026-09-19 | M5 · Explore filters | Nation lists every nation except UNK. Class options come from `vehicles.json`, limited to the selected type (the current class is always kept). Category labels are under `explore.category.*`. Sort is a radio Menu. The vehicle input is an ARIA combobox (8 matches, prefix first). Enter on unmatched text searches it as a code, and a second Escape clears the filter. The active-filters line is hidden rather than unmounted, and focus moves to the next filter, else the previous one, else the first control. | Keyboard flow; codes missing from the seed list still work. |
| 2026-09-19 | M5 · Explore paging and states | TanStack Virtual over card lines (250px min, 16px gap). `fetchNextPage` fires within 2 lines of the end, only when a next page exists and nothing is loading or failed. A skeleton line shows while a page loads. A failed page shows "Couldn't load more skins." with Retry and keeps the grid. With `?many=1`: 1,284 results, ~24 cards in the DOM. Initial load shows 8 skeleton cards. Offline shows the EmptyState with Open My Hangar and Retry (a second failure toasts "Still offline…"). The previous results stay while a filter change loads (`aria-busy`). | BUILD_PLAN M5 states. |
| 2026-09-19 | M5 · Explore card states | Done steps are ink-3 with a check, the current step amber, upcoming steps ink-4 (not the prototype's ink-5: 10px text needs 4.5:1). There is a `role=progressbar` with `aria-valuetext`. Errors are localized by code (network / unavailable / conflict / notFound; other codes show the backend text); a conflict offers "Install as a copy" (never Replace from a card). A temporary install shows "Trying in game" / "Keep or discard it on the skin page". Focus stays on the card after the pressed button goes away. | Contrast and a clear way out of each state. |
| 2026-09-19 | M5 · Following tab | `following_mark_seen` runs when the user **leaves** the tab after new skins were shown (switching tab, clicking a follow card, another section), not on opening and not when opening a detail from it. A vehicle follow card applies the vehicle filter; an author card searches the author. The sub-line reads 'Author · N skins on WT Live' when a cached skin carries `author.skinCount`, else 'Author'. Unfollow is undoable and the Undo restores the previous `lastSeenAt`; if that fails, a toast says so. | The user sees the counts before they reset. |
| 2026-09-19 | M5 · Palette | Skin results come from every WT Live skin in the query cache (search pages, posts, following-new), read when the palette opens, and open the Skin detail. Vehicle results apply the Explore vehicle filter. 3 of the 9 places are kept for matching vehicles and actions, so thousands of cached skins never hide them. | Closes the M1 palette TODOs. |
| 2026-09-19 | M5 · Formatting | Compact counts follow the UI language ('24.1k', '24,1k'). Full counts use Intl ('24,120' / '24.120'). Dates in both Explore and the detail are day-month-year with a short month. | Consistent numbers and dates. |
| 2026-09-19 | M5 · Skin detail structure | UI-only store `src/store/detail.ts`, reset whenever the detail opens or the skin changes. Tabs follow the WAI-ARIA pattern with automatic activation. Thumbnails are a radiogroup with roving tabindex. Escape closes zoom, then compare, one layer per press, only on the Gallery tab and never under the palette. `useFocusFallback` moves focus only when it fell to `<body>`. Loading/offline/error keep the top bar with just the back button ("Back to Explore"). | Predictable keyboard and focus. |
| 2026-09-19 | M5 · Skin detail · Try in game | Try state is derived from installs and `useHangar({ includeTemporary: true })`: idle / installing / active / **kept** (already in My Hangar, no Try button). The detail opens on the Try tab when that skin is being tried, unless the user picked a tab. While a normal install runs, Try is `aria-disabled` with a reason. Keep/Discard call `finalize_try` with the WT Live id via `mutateAsync`, so the toast appears even after leaving the screen. Discard has no Undo because it is itself the undo of the try. The installing bar is `motion-safe:animate-indeterminate` and stays still under reduced motion. | README §3; no spinners. |
| 2026-09-19 | M5 · Skin detail · side panel | A **vehicle Follow** toggle sits on the VEHICLE label row, next to the card button and never inside it (the README designs only the author Follow; BUILD_PLAN M5 needs vehicles). This moves the card down 11px. Follow toggles carry their state in the accessible name ('Follow X' / 'Following X'), with no `aria-pressed`. Unfollow is undoable and restores `lastSeenAt`. "Open original post" copies the URL until the opener plugin is approved. The vehicle card applies the Explore vehicle filter. Stats: DOWNLOADS/LIKES at 500 15px, CATEGORY/POSTED at 13px (prototype). "Add to collection" is `aria-disabled` ("Install it first") until installed; after that it is a Menu that marks the collections already holding the skin. Adding has no Undo because it isn't destructive. | Prototype 1:1 where possible; the dependency rule. |
| 2026-09-19 | M5 · Skin detail · Gallery and Textures | Compare candidates come from `useWtLiveSearch({ vehicle, sort: 'downloads', page: 0 })`, excluding the skin itself. Compare shows only when there are candidates and leaves compare mode if they disappear. Placeholder views are Front/Side/Rear/Detail. The textures tab calls `read_textures` with `skinId` when installed, else `wtliveId`, keeping the previous rows while the next arrive. Warnings are localized by `warningKind` and linked with `aria-describedby`. The table is semantic with fixed 40/20/20/20 columns. The swatch is 28px border-box, not the prototype's 30px content-box. Corner marks are 9px. | README §3. |
| 2026-09-19 | M5 · Known visual deltas | Keep button padding is 16px (prototype 18px). The temporary card uses an `amber-10` fill and `amber-35` border (prototype amber/.06, .4), and its dot uses `pulse6` (1.2s; prototype 1.6s). | `cn()` doesn't merge Tailwind classes; not worth the risk. |
| 2026-09-19 | M6 · Settings structure | The nav is a WAI-ARIA vertical tablist with automatic activation and a single tabpanel (`tabIndex=0`, scrolls). The open section, a pending Clear and "refocus Change" live in `src/screens/Settings/settingsUi.ts`: they survive leaving Settings but are not persisted. The content column is 704px (640 + 32px padding each side, as the prototype's content-box renders). Conflicts and Language use one `RadioCards` radiogroup (roving tabindex, wrap, Home/End) with a 16px ring and an ink-5 border (3:1). | README §7 measured 1:1 against the prototype. |
| 2026-09-19 | M6 · Unavailable controls | Start with Windows, Check now, Install updates automatically, Source code and Report a problem use `aria-disabled`, stay focusable, and explain why in their helper or title. Switches keep showing the stored values. | Keyboard and screen-reader users find the reason. |
| 2026-09-19 | M6 · Reduce motion | 'on' sets `data-reduce-motion="true"` (index.css stops animations). 'off' sets `"false"` and adds a `<style data-livery-motion>` with a copy of every `prefers-reduced-motion: no-preference` rule without that query, so the app animates even when Windows asks for less. 'system' removes both. The copy is taken when the mode is applied (in dev, reload after editing CSS). | A real override without touching components. |
| 2026-09-19 | M6 · Language | `useLanguageSync` applies the language actually shown: de/ru/fr → 'en' for `<html lang>`, plurals and number formats. The radio shows "English for now" as its description. Adding a language to `translated` in `src/i18n/index.ts` enables it. | No German number format inside English text. |
| 2026-09-19 | M6 · Toasts onExpire | `toast.undoable(message, onUndo, onExpire?)`: `onExpire` runs exactly once when the toast leaves without Undo (timeout, ×, pushed out of the stack of 4, `clear()`), never after Undo or Ctrl+Z. Its errors are logged. | Deferred commits for permanent actions. |
| 2026-09-19 | M6 · Backups → Clear | A deferred commit: the list empties at once, the toast offers Undo for 6 s, and on expiry `clear_backups({ ids })` removes only the backups listed when Clear was pressed (one made during the window survives). A failure shows the error and brings the list back. | Clear is permanent, but Undo is required. |
| 2026-09-19 | M6 · Game folder | Change/Choose opens First run in 'choose' mode (`startFirstRun({ step: 'choose', returnTo: 'settings' })`), without detection. There, a single "Back to Settings" link replaces "Skip for now" and "Back": it cancels without detecting, writing or toasting. Focus comes back to Change. Picking a folder that has skins leads to the usual import step. Paths stay hidden behind "Show path"; the watched folder shows its base name or "Downloads (default)". Settings changes are optimistic and fall back with a toast on failure. The version comes from `getVersion()` in Tauri, else from `package.json` (only `version` is bundled). | Paths hidden by default (CLAUDE.md). |
| 2026-09-19 | M6 · About · Licenses | An in-app modal: focus trap, Escape closes without propagating, focus returns to the opener. The OFL 1.1 texts for Geist and IBM Plex Mono are imported `?raw` behind disclosures. Library names and SPDX ids are not translated. No logos. | README §7. |
| 2026-09-19 | Tokens (M5/M6) | `bg.tag` rgba(15,16,18,.85) (Explore category tag), `boxShadow.glow-lg` 0 0 10px amber/.6 (About brand dot), `keyframes/animation.indeterminate` (Try in game bar), `placeholder-stage` / `placeholder-stage-alt` stripes (detail main image and compare panes). | Values from the prototype missing from the token file. |
| 2026-09-19 | M5 · following_set lastSeenAt | `following_set` takes an optional `lastSeenAt` (RFC 3339). With `follow: true` it becomes the entry's `lastSeenAt` (new or existing), stored as given. It is validated even on unfollow (invalidInput "Not an RFC 3339 time"). The Undo of an unfollow passes it, so "N new" comes back exactly. `useMarkFollowingSeen` also invalidates the following-new query, whose key holds only ids. | Undo restores the exact state. |
| 2026-09-19 | M5 · Temporary skins in collections | Activating a collection leaves Try in game (temporary) skins where they are: not activated, not deactivated, not moved. `finalize_try` is local and never emits `net://status`. | A try must not vanish from the game mid-test. |
| 2026-09-19 | M6 · clear_backups ids | `clear_backups({ ids? })`: without ids it clears every backup and sweeps leftovers, as before. With ids it removes only those; unknown ids are ignored and leftovers are not swept. Collection members that are gone for good leave their collections either way. | The deferred Clear must not delete a backup made during its Undo window. |
| 2026-09-19 | M6 · Library per game folder | The index is one file per game folder: `<appData>/library/<key>.json`, where key is 16 hex digits of FNV-1a 64 over `game::root::path_key(root)` (pinned by a test). Each file also records `gameRoot` for readers. The managed `library::Libraries` loads each folder's index on first use (missing → empty, corrupt → `.json.bad`) and keeps one store per folder for the session. Commands read the settings once and act on that folder's `GameLibrary`. A background install keeps its own folder's store. With no game folder saved, hangar/collections/backups read empty and collection changes answer invalidInput "No game folder set". `LibraryStore::detached()` (read-only, empty) serves queue clash checks without a folder. | Changing the game folder must show that folder's skins, and switching back restores the old ones with their collections and backups. |
| 2026-09-19 | M6 · Library migration | At launch the old `<appData>/library.json` is renamed into the saved folder's index, only while `library/` holds no index yet. If the rename fails, it is copied and the original becomes `library.json.migrated`. It never migrates twice. | Existing installs keep their library. |
| 2026-09-19 | M6 · After set_game_path | The queue re-checks every waiting item against the new folder (changed rows are re-sent through `queue://added`), and the new folder's stale staging and expired backups are purged. The watcher reads the settings at every poll, so the new UserSkins is a fresh baseline, not a change. The frontend invalidates settings, hangar, collections and backups (`BACKUPS_KEY` lives in `src/queries/settings.ts`). A folder saved through `set_settings({ gamePath })` is followed by the library but doesn't trigger the queue re-check; the UI never saves it that way. | The UI shows the new folder at once. |
| 2026-09-19 | Dev · Mock backend (M6) | The mock keeps one library per game folder too. The prototype library goes to the first folder saved; other folders start empty plus the two disk-only folders. Two documented differences from Rust: no queue re-check after a switch, and an install running during a switch lands in the new folder. | Browser checks of the folder switch. |
| 2026-09-19 | Rust formatting | `src-tauri/rustfmt.toml`: `max_width = 120`, `use_small_heuristics = "Max"` (the style the code already had); `cargo fmt --check` is clean. | One formatter setting for every session. |
